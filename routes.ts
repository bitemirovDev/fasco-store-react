const publicRoutes = ['/'];
const apiAuthPrefix = '/api/auth';
const authRoutes = ['/auth/login', '/auth/register'];
const DEFAULT_REDIRECT_ROUTE = '/';

export { publicRoutes, apiAuthPrefix, authRoutes, DEFAULT_REDIRECT_ROUTE };
